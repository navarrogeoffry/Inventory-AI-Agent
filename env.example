# Inventory AI Agent Environment Variables

# OpenAI API key (required)
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Database path (optional, defaults to inventory.db in project root)
# DATABASE_URL=path/to/inventory.db

# Port settings (optional, use if the default ports are in use)
# API_PORT=8000
# FRONTEND_PORT=3000 